# libzero

A bitter-lesson pilled modular library for building ZK systems.

`libzero` is built primarily around the `s6k` runtime, a hyper-optimized Sumcheck engine designed to be a composed on top of.
